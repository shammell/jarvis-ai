You are a principal engineer and AI systems researcher (NeurIPS/ICML level).
I am giving you targeted files from my JARVIS autonomous agent codebase.
Your job is NOT a full overhaul — it is a SURGICAL, PRIORITIZED, VERIFIABLE
improvement of the specific files I provide.

=== GROUND RULES (NON-NEGOTIABLE) ===
1. Only modify files I explicitly paste. Never invent file contents.
2. Every change must cite WHY (bug fix / perf / safety / research upgrade).
3. Output must be copy-paste ready. No placeholders, no "# TODO".
4. If a change requires another file to change, flag it as a dependency.
5. Rate each change: [CRITICAL BUG] [SECURITY] [PERF] [RESEARCH UPGRADE] [CLEANUP]

=== CURRENT KNOWN ISSUES (verified by me) ===

CRITICAL BUG #1 — jarvis_brain.py Executor.shell():
  *cmd_args AND full_cmd both passed to create_subprocess_exec → TypeError on every shell call.
  Fix: Remove redundant full_cmd argument.

CRITICAL BUG #2 — Security blocklist (jarvis_brain.py):
  Blocks only ["rm -rf /", "format C:", "shutdown"] — trivially bypassed.
  Fix: Implement proper sandboxed execution with allowlist approach.

ARCHITECTURE GAP #1 — LLM provider (llm_provider.py):
  Groq is hardcoded as only provider. No fallback chain.
  Fix: Provider abstraction with priority queue: Groq → Anthropic → local llama.cpp

RESEARCH GAP #1 — core/speculative_decoder.py:
  Claims "speculative decoding" but is actually just Groq API + semantic string matching.
  Real speculative decoding (Leviathan et al. 2023) requires draft model + verifier model.
  Since we use API-only inference, implement BanditSpec-style adaptive batching instead:
  - Use historical acceptance rates per query-type to dynamically size draft batches
  - Track per-query-type latency/cost to optimize API call batching
  This is honest AND gives real speedup on API-based systems.

RESEARCH GAP #2 — memory/graph_rag.py:
  Basic networkx graph with no dynamic community evolution.
  Upgrade: Implement incremental Louvain with decay factor for old edges
  (temporal graph RAG — nodes lose weight over time, recent context stays hot).

SAFETY GAP #1 — core/self_evolving_architecture.py:
  Self-modification loop has no gating mechanism.
  Fix: Every proposed self-modification must pass:
    (a) Z3 formal constraint check (z3_logic.py exists — use it)
    (b) Unit test suite on proposed change
    (c) Semantic diff review logged to SECURITY_AUDIT.json
  Only then apply. Human-in-loop flag for changes above risk_score > 7.

=== THIS SESSION'S SCOPE (do ONLY these, in this order) ===

Priority 1 — Fix the two CRITICAL BUGS. These crash the system.
Priority 2 — Implement proper shell sandboxing (allowlist > blocklist).
Priority 3 — LLM provider abstraction with fallback chain.
Priority 4 — BanditSpec-style adaptive batching for speculative_decoder.py
Priority 5 — Temporal decay for graph_rag.py

=== FILES I AM PASTING BELOW ===
[PASTE: jarvis_brain.py]
[PASTE: core/speculative_decoder.py]
[PASTE: memory/graph_rag.py]
[PASTE: core/llm_provider.py]

=== OUTPUT FORMAT (strict) ===

For each file, output:

## FILE: filename.py
### Changes made:
- [CRITICAL BUG] Line X: <what was wrong> → <what you did>
- [SECURITY] ...
- [RESEARCH UPGRADE] ...

### Dependencies flagged:
- <list any other files that need updating as a result>

### Full corrected code:
```python
# complete production-ready code here
```

### Test to verify this fix works:
```python
# minimal pytest test that would have caught this bug
```

---

After all Priority 1-5 files, output:

## NEXT SESSION PROMPT
A refined version of this prompt for the next session,
pre-loaded with: remaining issues, updated known state,
and next 5 priorities.

=== BEGIN ===
[Paste your files here and Claude will execute]